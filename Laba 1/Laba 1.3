#include <stdio.h>
#include <math.h>

int main()
{
	/*float a, r;
	printf("Input number a,r\n");
	scanf_s("%g", &a);
	scanf_s("%g", &r);
	if (r <= a / 2)
		printf("The circle will fit into a square\n");
	   else
		printf("The circle will NOT fit into a square\n");
		if (r >= a * sqrt(2) / 2)
			printf("The square will fit in the circle\n");
		else
			printf("The square will NOT fit in the circle\n");
			*/
	float a, r;
	printf("Input number a,r\n");
	scanf_s("%g", &a);
	scanf_s("%g", &r);
	{
		if (r <= a / 2)
			printf("The circle will fit into a square\n");
		else if (r < a * sqrt(2) / 2)
			printf("The square will not  fit in the circle\n"); //a= 4 r = 2.5
		else
			printf("The square will fit in the circle\n");       
	}
	return 0;            
	 
}
